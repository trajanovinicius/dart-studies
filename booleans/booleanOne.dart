//Boolean é o simples: TRUE ou FALSE que conhecemos.

void main() {
  int idade = 26;
  double altura = 1.80;
  bool geek = (idade ==
      altura); // aqui fazemos uma comparação, o dart devolve para a gente a respota verdadeira sobre ela.
  print(geek);
}
// definimos uma variável do tipo boolean com a palavra bool antes da declaração do nome.